/**
 * Barrel for the connectors' authenticated Meteor methods. Importing this (from
 * apps/meteor/app/connectors/server/index.ts) registers every method as a server-boot side-effect.
 */
import './getAuthorizeUrl';
